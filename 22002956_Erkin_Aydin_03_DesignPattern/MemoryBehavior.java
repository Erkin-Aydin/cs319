public interface MemoryBehavior {
    /**
     * This method is for implementation purposes. Its implementations should format the parameter and return it.
     * @param info contents of a directory.
     * @return a formatted display of the contents.
     */
    public String showMemory(String info);
}
